import 'package:flutter/material.dart';

/// Moni 브랜드 팔레트 — "Mint Ledger"
class AppColors {
  AppColors._();

  static const primary = Color(0xFF00BFA0);
  static const primaryDark = Color(0xFF00816B);
  static const ink = Color(0xFF10221E);

  static const expense = Color(0xFFD32F2F);
  static const income = Color(0xFF1976D2);

  static const background = Color(0xFFF2FBF8);
  static const surface = Colors.white;

  static const textPrimary = Color(0xFF10221E);
  static const textMuted = Color(0xFF6B6D76);
  static const divider = Color(0x14000000);
}
